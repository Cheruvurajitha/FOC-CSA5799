#include <stdio.h>

int main() {
    int limit, sum = 0;

    printf("Enter the limit: ");
    scanf("%d", &limit);

    for (int i = 1; i <= limit; i += 2) {
        sum += i * i;
    }

    printf("Sum of squares of odd numbers up to %d is: %d\n", limit, sum);

    return 0;
}
