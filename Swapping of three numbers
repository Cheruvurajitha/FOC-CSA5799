#include <stdio.h>

int main() {
    int a, b, c;
    printf("Enter three numbers: ");
    scanf("%d %d %d", &a, &b, &c);

    int temp;
    temp = a;
    a = c;
    c = b;
    b = temp;

    printf("After swapping: a = %d, b = %d, c = %d\n", a, b, c);
    return 0;
}
